"""Blender worker sub-package.

Each module handles one concern:

* :mod:`scene`  – scene setup and SVG import
* :mod:`engrave` – ``carve_plaque`` pipeline runner
* :mod:`insert`  – ``build_inserts`` pipeline runner
* :mod:`export`  – ``.blend`` and per-layer STL export
"""
